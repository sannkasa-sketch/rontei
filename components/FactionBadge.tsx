const styles: Record<string,string> = {"導入賛成":"border-blue-200 bg-blue-50 text-blue-800","導入反対":"border-rose-200 bg-rose-50 text-rose-800","条件付き賛成":"border-amber-200 bg-amber-50 text-amber-800","規制重視":"border-violet-200 bg-violet-50 text-violet-800"};
export function FactionBadge({ name, previousName }: { name: string; previousName?: string }) { return <span className={`inline-flex items-center gap-1 rounded-full border px-2.5 py-1 text-[11px] font-bold ${styles[name] ?? "border-slate-200 bg-slate-50 text-slate-700"}`}><span>{name}</span>{previousName && <span className="font-semibold opacity-60">← {previousName}</span>}</span>; }
